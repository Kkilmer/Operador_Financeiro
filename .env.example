DATABASE_URL="postgresql://operador:senha_segura@localhost:5433/operador_financeiro"
DATABASE_URL_SQLITE="file:./prisma/dev.db"
APP_URL="http://127.0.0.1:3000"

# PostgreSQL para uso futuro com Docker Compose e Prisma
POSTGRES_DB=operador_financeiro
POSTGRES_USER=operador
POSTGRES_PASSWORD=senha_segura
DATABASE_URL_POSTGRES="postgresql://operador:senha_segura@localhost:5433/operador_financeiro"

# SMTP opcional para envio real de reset de senha
SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM="Operador Financeiro <no-reply@exemplo.com>"
